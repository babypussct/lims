import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.otada.lims',
  appName: 'lims',
  webDir: 'dist/lims-cloud-pro/browser'
};

export default config;
