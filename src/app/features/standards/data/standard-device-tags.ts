/**
 * Stable import surface for the read-only device catalog. The implementation
 * lives beside the pure tag helpers so the UI and tests share one source.
 */
export {
  STANDARD_DEVICE_OPTIONS,
  normalizeDeviceAlias,
} from '../services/standard-tag.utils';
