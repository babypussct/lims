# UI/UX remediation checklist

Phạm vi nguồn: audit giao diện toàn bộ `src/app` ngày 2026-08-13. Checklist này theo dõi remediation, không thay thế kiểm tra nghiệp vụ hoặc kiểm tra production.

Nguyên tắc: chỉ đánh dấu sau khi có thay đổi mã nguồn và bằng chứng kiểm tra tương ứng. Kiểm tra light/dark trên trình duyệt, keyboard-only, authenticated runtime và production vẫn để mở nếu chưa có bằng chứng đúng môi trường.

## Phase 0 — Nền tảng và quy ước

- [x] Ghi nhận palette xám chính là `slate`, primary action là `indigo`, thang radius/shadow/khoảng cách và nhãn nút trong [UI_CONVENTIONS.md](UI_CONVENTIONS.md).
- [x] Ghi rõ nguyên tắc không thêm shade Tailwind chưa khai báo và dùng `fa-xmark` cho nút đóng.
- [x] Giữ lại có chủ ý các token Soft UI (`gradient-soft`, `shadow-soft-*`, palette `gray`) vì chúng vẫn có consumer thực tế ở shell/dashboard/public/auth và tạo visual identity riêng cho các surface Soft UI. Quy ước mới tiếp tục ưu tiên `slate` cho vùng UI mới; không mở rộng thêm consumer Soft UI nếu không có lý do sản phẩm.

## Phase 1 — Quick wins

- [x] Thay toàn bộ `slate-105`, `slate-205`, `slate-555`, `slate-855` trong `src/app` bằng shade chuẩn theo ngữ nghĩa vị trí.
- [x] Thay toàn bộ `fa-times` trong `src/app` bằng `fa-xmark`.
- [x] Bổ sung biến thể dark mode cho shell, header, danh sách, form, input, modal và footer của `target-group-manager`.
- [x] Bổ sung biến thể dark mode cho shell, header, bảng, form, import preview và modal của `master-target-manager`.
- [x] Bổ sung biến thể dark mode cho danh sách công thức, form, search result, modal và footer của `recipe-manager`.
- [x] Đổi primary action của phần đã chạm trong Targets/Recipes sang indigo; giữ màu ngữ nghĩa teal/purple chỉ ở trạng thái hoặc chi tiết phụ.
- [x] `mobile-qr-login` giữ scanner nền đen để bảo toàn contrast/camera usability; trạng thái xác thực, xác nhận, user card và security notice đã có dark-mode tương ứng. CTA xác nhận/hủy dùng primitive button; header scanner và scanner viewport vẫn là boundary chuyên biệt.

## Phase 2 — UI primitives

- [x] Tạo và test `<app-button variant="primary|secondary|danger|ghost" size="sm|md">`.
- [x] Tạo và test `<app-page-header>` với heading, icon, subtitle, action slot và tùy chọn sticky.
- [x] Tạo và test `<app-modal-shell>` tích hợp `appModalA11y`, Escape, focus, footer và animation chuẩn.
- [x] Tạo và test `<app-empty-state>`.
- [x] Tạo và test `<app-toolbar>`.
- [x] Tạo route demo nội bộ cho primitives với light/dark và các biến thể chính.

## Phase 3 — Di trú theo module

Mỗi dòng chỉ được đánh dấu khi module đã dùng primitive tương ứng và đã chạy kiểm tra riêng. Quy ước ô: `[ ]` = primitive có áp dụng nhưng chưa hoàn tất/bằng chứng chưa đủ; `N/A` = màn hình không có semantic tương ứng; `Exception` = có boundary chuyên biệt được giữ có chủ ý thay vì ép qua primitive chung.

| Module | Page header | Toolbar | Modal shell | Button/footer | Empty/skeleton |
|---|---:|---:|---:|---:|---:|
| dashboard | [x] | [x] | N/A | [x] | [x] |
| inventory | [x] | [x] | Exception | [x] | [x] |
| standards (+ requests) | [x] | [x] | [ ] | [ ] | [ ] |
| results / SOP entry | [x] | [x] | [x] | [x] | [x] |
| batch | [x] | [x] | [x] | [x] | [x] |
| preparation | [x] | N/A | N/A | [x] | N/A |
| targets | [x] | [x] | [x] | [x] | [x] |
| recipes | [x] | [x] | [x] | [x] | [x] |
| documents | [x] | [x] | Exception | [x] | [x] |
| checklist | [x] | [x] | N/A | [x] | [x] |
| labels | [x] | N/A | N/A | [x] | N/A |
| config | [x] | [x] | [x] | [x] | [x] |
| SOP editor/calculator | [x] | [x] | [x] | [x] | [x] |
| traceability | [x] | N/A | N/A | [x] | [x] |
| auth/public | [x] | N/A | Exception | [x] | N/A |

## Phase 4 — Guardrails

- [x] Thêm `test:ui-guardrails` và nối vào `npm test` để chặn shade không có trong Tailwind config, `fa-times` và native fullscreen overlay mới ngoài primitive modal; overlay legacy hiện tại được khóa bằng baseline để không tăng thêm.
- [x] Thêm mục UI vào `.github/pull_request_template.md`, yêu cầu đối chiếu [UI_CONVENTIONS.md](UI_CONVENTIONS.md), chạy guardrail và ghi nhận light/dark, keyboard focus, narrow screen khi áp dụng.
- [x] Giữ route `__ui-primitives` dev-only làm nguồn tham chiếu trực quan; contract test xác nhận route không được mở trong production.

## Verification evidence — 2026-08-13

- [x] `rg` xác nhận không còn `slate-105|slate-205|slate-555|slate-855` trong `src/app`.
- [x] `rg` xác nhận không còn `fa-times` trong `src/app`.
- [x] `git diff --check` pass.
- [x] `npx.cmd ngc -p tsconfig.app.json --noEmit` pass.
- [x] `npm.cmd run build` pass; release notes validation xác nhận `v26.08.13-b01`.
- [x] ESLint phạm vi các file TypeScript đã chạm pass; `standards-internal-id-sync-modal.component.ts:211` được loại khỏi targeted run vì là lỗi baseline ngoài dòng icon đã đổi.
- [x] `npm.cmd run test:ui-primitives` pass 5/5 contract tests cho button, page header, modal shell, empty state, toolbar và route demo dev-only.
- [x] `npm.cmd run test:ui-dashboard` pass 2/2 contract tests: Dashboard dùng page header/button/empty state dùng chung; Statistics dùng page header/toolbar/button dùng chung; sweep cuối xác nhận các nhãn Statistics đã migrate tuân thủ sentence case.
- [x] Sau migration Dashboard/Statistics, `npx.cmd ngc -p tsconfig.app.json --noEmit`, ESLint targeted cho `dashboard.component.ts`, `statistics.component.ts`, `dashboard-ui-primitives.contract.test.ts` và `git diff --check` đều pass; scan không còn các chuỗi Title Case mục tiêu trong `statistics.component.html`.
- [x] `npm.cmd run build` được chạy lại sau sweep Dashboard/Statistics và trả `exit 0`; release notes validation xác nhận `v26.08.13-b01`, Angular bundle generation hoàn tất.
- [x] Inventory đã migrate sang `app-page-header`, `app-toolbar`, `app-button` và `app-empty-state`; `npm.cmd run test:ui-inventory` pass 2/2, `npx.cmd ngc -p tsconfig.app.json --noEmit`, ESLint targeted và `git diff --check` đều pass; `npm.cmd run build` trả `exit 0` và xác nhận `v26.08.13-b01`.
- [x] Standards, Standards Requests, Standard Detail và Standard Usage đã migrate page header sang `app-page-header`; vùng filter/search chính của Requests và Usage dùng `app-toolbar`; action thông thường dùng `app-button`; Detail/Usage dùng `app-empty-state`; các control chuyên biệt như menu trigger, segmented tabs, export option-card, filter-chip dismiss và icon-only delete vẫn giữ native để bảo toàn semantics/layout. Nhãn mục tiêu đã chuyển sang sentence case và Detail không còn page-level `<h1>` trùng với shared page header. `npm.cmd run test:ui-standards` pass 4/4; `npx.cmd ngc -p tsconfig.app.json --noEmit`, ESLint targeted cho các TypeScript đã chạm và `npm.cmd run build` đều trả `exit 0`; build xác nhận `v26.08.13-b01`.
- [x] Recipes đã migrate `app-page-header` + `pageHeaderActions`, search sang `app-toolbar` + `toolbarSearch`, action thường sang `app-button`, trạng thái danh sách rỗng sang `app-empty-state` và modal tạo/cập nhật sang `app-modal-shell`; contract test khóa đủ năm primitive này, trong khi các nút icon-only sửa/xóa recipe và xóa dòng thành phần vẫn giữ native để bảo toàn layout chuyên biệt. `npm run test:ui-recipes` pass 1/1 ngày 2026-08-14. Runtime smoke của Recipe Manager vẫn theo dõi riêng ở nhóm visual/keyboard bên dưới, không được suy ra từ contract test.
- [x] Targets đã migrate hai manager sang `app-page-header`; action header dùng `pageHeaderActions`, ô tìm kiếm của Master Targets dùng `app-toolbar`/`toolbarSearch`, action thường dùng `app-button`, trạng thái rỗng/tìm kiếm rỗng dùng `app-empty-state` và ba modal hiện có dùng `app-modal-shell`. Tiêu đề header mục tiêu dùng sentence case. `npm.cmd run test:ui-targets` pass 2/2; `npx.cmd ngc -p tsconfig.app.json --noEmit`, ESLint targeted cho hai manager + contract test và `git diff --check` đều pass; `npm.cmd run build` trả `exit 0` và release notes validation xác nhận `v26.08.13-b01`.
- [x] 2026-08-14: `npm run test:ui-guardrails` pass trên 277 file production: 0 shade Tailwind không hợp lệ, 0 `fa-times`; sau migration Result Entry còn 66 fullscreen overlay legacy và baseline đã được hạ tương ứng để hai overlay vừa loại bỏ không thể quay lại mà vẫn pass guardrail. Sau đó Standards Requests tiếp tục loại bỏ thêm 6 overlay tự dựng (5 action modal + 1 purchase-request modal), đưa hiện trạng xuống 60 overlay legacy.
- [x] 2026-08-14: `git diff --check` pass sau khi loại bỏ trailing whitespace ở 6 SOP entry template; cảnh báo LF→CRLF của Git không phải lỗi diff.
- [x] 2026-08-14: Preparation/Smart Prep đã dùng `app-page-header` + `pageHeaderActions`; các action chuẩn ở header, khu vực dãy chuẩn/QC, phần kết quả và footer dùng `app-button`. Nút xóa icon-only và control chọn tác vụ tính toán chuyên biệt vẫn giữ native có chủ ý để bảo toàn semantics/layout. `npm.cmd run test:ui-preparation` pass 1/1, `npm.cmd run test:prep` pass 33/33, `npm.cmd run test:ui-guardrails` pass, và `npm.cmd run build` trả `exit 0` với release notes `v26.08.13-b01`.
- [x] 2026-08-14: Batch/SmartBatch đã migrate workflow-level CTA và layout/state chung sang `app-button`, `app-page-header`, `app-toolbar`, `app-modal-shell` và `app-empty-state`; các control chuyên biệt như group card, delete/preview, target-group picker, checkbox, SOP card có `aria-pressed` và SOP `<select>` vẫn giữ native có chủ ý. CSS `smartbatch-primary-action*` đã được loại bỏ và contract test khóa ranh giới primitive này. `npm.cmd run test:smart-batch` pass 46 source/contract tests + 22 Firestore emulator tests, `npm.cmd run test:ui-primitives` pass 5/5, `npm.cmd run test:ui-guardrails` pass trên 277 file với 68 overlay legacy, `npx.cmd ngc -p tsconfig.app.json --noEmit`, ESLint targeted, `git diff --check` và `npm.cmd run build` đều pass; build xác nhận `v26.08.13-b01`.
- [x] 2026-08-14: Results/SOP entry hoàn tất migration generic chrome: Result List dùng `app-page-header`, `app-toolbar`, `app-button`, `app-empty-state`; Result Entry chuyển hai modal preflight/reset sang `app-modal-shell`, action footer/back sang `app-button` và trạng thái không tìm thấy dữ liệu sang `app-empty-state`. `result-entry-header`, prefix tabs, SOP outlet, Excel import và các stateful/bulk input control vẫn giữ boundary chuyên biệt có chủ ý. `npm run test:ui-results` pass 4/4 contract tests; `npm run test:ui-guardrails`, `npx ngc -p tsconfig.app.json --noEmit --pretty false`, targeted ESLint, full `npm run lint` và `npm run build` đều pass; build xác nhận `v26.08.14-b01`.
- [x] 2026-08-14: Standards Requests tiếp tục migrate 5 action modal (`approve`, `reject`, `return`, `logUsage`, `adminReceive`) và modal admin purchase requests sang `app-modal-shell`; footer/action chuẩn chuyển sang `app-button`, contract test khóa `AppModalShellComponent` và cấm `requests-modal-layer`. Baseline loại bỏ 6 quota Standards Requests và 4 quota Batch đã stale; `ui-guardrails.js` nay fail cả khi baseline còn quota dư sau migration. `npm run test:ui-standards` pass 4/4, `npm run test:ui-guardrails` pass trên 277 file với đúng 60 overlay legacy, `npx ngc -p tsconfig.app.json --noEmit --pretty false` và `git diff --check` đều pass.
- [x] 2026-08-14: Standards utility modal batch 1 đã migrate `standards-history-modal`, `standards-bulk-tag-modal` và `standards-tag-manager-modal` sang `app-modal-shell`; toàn bộ business logic/output hiện hữu được giữ nguyên, custom fullscreen wrapper được loại bỏ và contract test khóa ba component khỏi quay lại `fixed inset-0`. `npm run test:ui-standards` pass 5/5, `npm run test:ui-guardrails` pass trên 277 file với đúng 57 overlay legacy, `npx ngc -p tsconfig.app.json --noEmit --pretty false` và `git diff --check` đều pass.
- [x] 2026-08-14: Standards purchase modal đã migrate `standards-purchase-modal` sang `app-modal-shell`; `purchaseForm`, validation, `ngSubmit`, `submitPurchaseRequest()`, output đóng modal, processing state và payload/service call được giữ nguyên. Nút submit ở footer dùng thuộc tính `form` để vẫn kích hoạt form hiện hữu; contract test khóa modal khỏi quay lại custom `fixed inset-0`. `npm run test:ui-standards` pass 5/5, `npm run test:ui-guardrails` pass trên 277 file với đúng 56 overlay legacy, `npx ngc -p tsconfig.app.json --noEmit --pretty false` và `git diff --check` đều pass.
- [x] 2026-08-14: Standards add/edit form modal đã migrate `standards-form-modal` sang `app-modal-shell`; toàn bộ `FormGroup`, tag picker, Drive upload, save/update flow và các action `Lưu & Thêm tiếp`/`Lưu thay đổi` được giữ nguyên. `closeOnBackdrop=false` được đặt trên shell để không mở rộng hành vi đóng modal so với overlay cũ; contract test khóa component khỏi quay lại custom fullscreen wrapper. `npm run test:ui-standards` pass 5/5, `npm run test:ui-guardrails` pass trên 277 file với đúng 55 overlay legacy, `npx ngc -p tsconfig.app.json --noEmit --pretty false` và `git diff --check` đều pass.
- [x] 2026-08-14: Standards assign/borrow modal đã migrate `standards-assign-modal` sang `app-modal-shell`; form signal, FEFO hint, user selection, purpose quick actions, processing state và payload `confirm` được giữ nguyên. Layout tóm tắt chuẩn hai cột vẫn nằm trong `modalBody`, action đóng/xác nhận chuyển sang footer chung và `closeOnBackdrop=false` giữ semantics backdrop cũ; contract test khóa component khỏi quay lại custom fullscreen wrapper. `npm run test:ui-standards` pass 5/5, `npm run test:ui-guardrails` pass trên 277 file với đúng 54 overlay legacy, `npx ngc -p tsconfig.app.json --noEmit --pretty false`, ESLint targeted và `git diff --check` đều pass.
- [x] 2026-08-14: Standards backfill modal đã migrate `standards-backfill-modal` sang `app-modal-shell`; user/date/amount/purpose/depleted state, tồn kho validation, quick actions và payload `BackfillData` được giữ nguyên. Layout tóm tắt chuẩn hai cột tiếp tục nằm trong `modalBody`, action đóng/ghi nhật ký chuyển sang footer chung và `closeOnBackdrop=false` giữ semantics backdrop cũ; contract test khóa component khỏi quay lại custom fullscreen wrapper. `npm run test:ui-standards` pass 5/5, `npm run test:ui-guardrails` pass trên 277 file với đúng 53 overlay legacy, `npx ngc -p tsconfig.app.json --noEmit --pretty false`, ESLint targeted và `git diff --check` đều pass.
- [x] 2026-08-14: Standards print modal đã migrate `standards-print-modal` sang `app-modal-shell`; layout cấu hình/preview hai cột, chế độ cuộn/A4, preset kích thước, field toggles, số bản in, điểm bắt đầu A4, QR và toàn bộ `printLabel()` DOM/print flow được giữ nguyên. Tiêu đề/mô tả động chuyển sang header chung, action hủy/in chuyển sang `modalFooter`, `size="xl"` giữ không gian preview lớn và `closeOnBackdrop=false` giữ semantics backdrop cũ; contract test khóa component khỏi quay lại custom fullscreen wrapper. `npx ngc -p tsconfig.app.json --noEmit --pretty false`, `npm run test:ui-standards` pass 5/5 và `npm run test:ui-guardrails` pass trên 277 file với đúng 52 overlay legacy.
- [x] 2026-08-14: Standards internal-ID sync modal đã migrate `standards-internal-id-sync-modal` sang `app-modal-shell`; auto-scan, filter/search, manual correction, confirmation/apply flow, toast và re-scan sau apply được giữ nguyên. Shared shell bổ sung `closeDisabled` để khóa nút đóng/Escape trong lúc scan hoặc apply mà không thay đổi mặc định của các modal khác; `closeOnBackdrop=false`, `size="xl"` và footer chung giữ layout/semantics của utility modal. Contract test khóa component khỏi quay lại custom `fixed inset-0`; baseline giảm còn 51 overlay legacy.
- [x] 2026-08-14: Standards bulk CoA modal đã migrate `standards-bulk-coa-modal` sang `app-modal-shell`; kết quả auto-match, selector ghép thủ công, trạng thái upload queue/progress/error và output `cancel`/`confirm` được giữ nguyên. `closeOnBackdrop=false` giữ semantics overlay cũ, `closeDisabled=isUploading` ngăn đóng bằng nút header/Escape khi upload đang chạy, `size="xl"` giữ không gian bảng ghép nối; contract test khóa component khỏi quay lại custom `fixed inset-0`. Baseline giảm còn 50 overlay legacy.
- [x] 2026-08-14: Standards import-data preview modal đã migrate `standards-import-data-modal` sang `app-modal-shell`; worksheet selector, thống kê valid/update/restore/warning, conflict/skip acknowledgement, preview table, tải CSV lỗi và output `cancel`/`confirm`/`sheetChange` được giữ nguyên. Shared shell mở rộng `size="2xl"` tương ứng `max-w-[96rem]` để không làm hẹp workspace import cũ; focus trap/Escape chuyển về `appModalA11y`, `closeOnBackdrop=false` và `closeDisabled=isImporting() || isParsing()` giữ semantics đóng khi đang parse/commit. Baseline giảm còn 49 overlay legacy.
- [x] 2026-08-14: Hai preview modal trong `standards-import-modal.component.ts` (import chuẩn và import nhật ký) đã migrate sang `app-modal-shell`; preview rows, date parsing display, valid/invalid/duplicate counts, mapping trạng thái và output `cancel`/`confirm` được giữ nguyên. Cả hai dùng `size="2xl"`, `closeOnBackdrop=false` và footer chung; nhãn action được chuẩn hóa sentence case. Contract test khóa file có đúng hai shell và không còn custom `fixed inset-0`; baseline giảm còn 47 overlay legacy.
- [x] 2026-08-14: Standards Requests `create-request-drawer` đã migrate sang `app-modal-shell`; search/filter, chọn chuẩn, FEFO warning, đề nghị mua, purpose form, payload `submitRequest` và reset semantics của `onClose()` được giữ nguyên. Shell dùng `size="xl"`, giữ backdrop-close hiện hữu, workspace hai cột nằm trong `modalBody`, action chuyển sang footer chung và các nhãn chính được chuẩn hóa sentence case. Contract test khóa component khỏi quay lại `requests-modal-layer`/`fixed inset-0`; baseline giảm còn 46 overlay legacy.
- [x] 2026-08-14: Standards data-cleanup modal đã migrate outer cleanup workspace sang `app-modal-shell`; các workspace CAS hợp lệ/placeholder/date-corrupted/invalid, PubChem lookup, chọn hồ sơ, save-current/save-next, history và undo batch tiếp tục giữ nguyên business/event contract. Lớp history nội bộ vẫn là absolute workspace bên trong shell để không đổi workflow; shell dùng `size="xl"`, `closeOnBackdrop=false` và `closeDisabled=isProcessing() || undoingBatchId() !== null`, đồng thời `onClose()` giữ nguyên reset/output semantics và chặn đóng khi đang undo. Contract test khóa component khỏi quay lại custom `fixed inset-0`; baseline giảm còn 45 overlay legacy.
- [x] 2026-08-14: Documents đã dùng `app-page-header`, `app-toolbar`, `app-button`, `app-empty-state` và `app-skeleton` cho generic page chrome, retry và loading/empty states. `documents-ui-primitives.contract.test.ts` khóa các primitive này và xác nhận preview tài liệu vẫn là fullscreen viewer boundary có `role="dialog"`, `aria-modal`, Escape/Tab handling, fullscreen state và focus restore riêng; chưa đánh dấu `Modal shell` vì `app-modal-shell` hiện áp max-height/header/body padding không phù hợp workspace viewer toàn màn hình. `npm run test:ui-documents` pass 2/2, `npm run test:ui-guardrails` pass với đúng 45 overlay legacy, `npx ngc -p tsconfig.app.json --noEmit --pretty false`, targeted ESLint và `git diff --check` đều pass.
- [x] 2026-08-14: Daily Checklist đã migrate generic standalone page chrome sang `app-page-header`, search/SOP filter sang `app-toolbar`, reset/retry/empty-state actions sang `app-button` và hai trạng thái rỗng sang `app-empty-state`. Embedded widget header, date previous/next + date picker, refresh/print control, view-mode segmented control, batch/card actions, progressive loading status (`loadedBatchCount()`) và toàn bộ `cl-print-*`/`#print-container` tiếp tục giữ custom/native có chủ ý để bảo toàn semantics nghiệp vụ và print layout. Module không có generic modal cần migrate nên `Modal shell` vẫn để trống. `npm run test:ui-checklist` pass 2/2, `npm run test:ui-guardrails` pass với đúng 45 overlay legacy, `npx ngc -p tsconfig.app.json --noEmit --pretty false`, targeted ESLint và `git diff --check` đều pass.
- [x] 2026-08-14: Labels/Label Print đã chuyển heading chính sang `app-page-header`, action lấy dữ liệu và hai CTA in sang `app-button`, chuẩn hóa sentence case và bổ sung dark-mode cho generic control chrome. Focus contract được sửa bằng cách loại bỏ `outline: none` cùng `ring`/`ring-color` CSS không hợp lệ để global `:focus-visible` hoạt động đúng. Mode selector, calibration controls, zoom và paper preview tiếp tục giữ native/custom vì là print-workstation semantics; vùng giấy preview cố ý giữ nền trắng để mô phỏng output. `Toolbar`, `Modal shell` và `Empty/skeleton` = `N/A` vì màn này không có semantic tương ứng.
- [x] 2026-08-14: Config bắt đầu migration theo ba manager master-data: `master-device-manager` và `matrix-type-manager` dùng `app-page-header`, `app-button`, `app-empty-state`, `app-modal-shell`; `sample-description-master` dùng thêm `app-toolbar` cho search/status filter và chuyển cả modal CRUD lẫn preview import sang shell chung. Bốn custom fullscreen overlay đã được loại bỏ; cả bốn shell mới đặt `closeOnBackdrop=false` để giữ semantics backdrop của modal cũ, action/heading mục tiêu được chuẩn hóa sentence case và file-picker/import flow vẫn giữ event contract hiện hữu. `config-ui-primitives.contract.test.ts` khóa các primitive, số modal, file input và việc không quay lại `fixed inset-0`; đây là bằng chứng cho giai đoạn đầu của migration Config. `npm run test:ui-config` pass 3/3, `npm run test:ui-primitives` pass 5/5, `npm run test:ui-guardrails` pass trên 277 file với đúng 41 overlay legacy, `npx ngc -p tsconfig.app.json --noEmit --pretty false`, targeted ESLint, `git diff --check` và `npm run build` đều pass; build xác nhận `v26.08.14-b01`.
- [x] 2026-08-14: Config tiếp tục với hai vi phạm fullscreen rõ nhất còn lại trong `config-roles` và `config-users`: chỉ outer modal chrome/footer được migrate sang `app-modal-shell`/`app-button`, `closeOnBackdrop=false` giữ hành vi backdrop cũ, còn `roleForm`, permission matrix, signal mở/đóng, save flow, role/status filter và batch control chuyên biệt được giữ nguyên. Hai quota tương ứng đã được xóa khỏi `ui-overlay-baseline.json`; `config-ui-primitives.contract.test.ts` khóa việc không quay lại `fixed inset-0` đồng thời xác nhận các contract form/filter/save/close còn nguyên. Hàng `config` trong bảng Phase 3 vẫn để `[ ]` vì chưa có đủ bằng chứng semantic để ép các tab/filter và màn Config còn lại qua primitive chung. `npm run test:ui-config` pass 5/5 và `npm run test:ui-guardrails` pass trên 277 file với đúng 39 overlay legacy.
- [x] 2026-08-14: Config General tiếp tục loại bỏ fullscreen overlay cuối cùng của module ở Recycle Bin: outer chrome/footer chuyển sang `app-modal-shell`/`app-button`, `closeOnBackdrop=false` giữ hành vi backdrop cũ; bảng dữ liệu, restore action, processing overlay và `emptyRecycleBin()` destructive workflow giữ nguyên boundary chuyên biệt. Quota `config-general.component.html` được xóa khỏi baseline, đưa tổng overlay legacy xuống 38; contract Config khóa shell/close/destructive contracts và cấm quay lại `fixed inset-0`.
- [x] 2026-08-14: Config tiếp tục hoàn tất generic chrome cho shell quản trị và các tab còn lại: `config.component` dùng `app-page-header`/`app-button`, Roles dùng `app-button` + `app-empty-state` cho danh sách và Users dùng `app-empty-state` cho kết quả lọc rỗng; form quyền, filter chip, batch action và bảng dữ liệu vẫn giữ boundary chuyên biệt. `npm run test:ui-config` pass 7/7, contract test được mở rộng để khóa page header/empty state, và typecheck/lint/guardrail pass.
- [x] 2026-08-14: SOP editor/calculator tiếp tục migrate generic action boundary: calculator dùng `app-button` cho footer workflow actions và editor dùng `app-button` cho back/save, ba selection workspace dùng `app-modal-shell`; tab editor, matrix/consumable picker và control nghiệp vụ vẫn giữ native/custom. `npm run test:ui-sop` pass 2/2; toàn bộ 3 quota overlay của `sop-editor.component.html` được xóa khỏi baseline, tổng overlay legacy còn 35.
- [x] 2026-08-14: Traceability dùng `app-page-header`, `app-button` và `app-empty-state` cho header, QR lookup actions, error recovery và link sang kết quả; lookup form, QR canvas, verification workspace và data card vẫn là boundary chuyên biệt. `npm run test:ui-traceability` pass 1/1; contract khóa ngSubmit/QR lookup semantics và token không hợp lệ.
- [x] 2026-08-14: Auth/public dùng `app-button` cho forbidden recovery và `app-page-header`/`app-button` cho privacy policy/terms; mobile QR login và password setup giữ custom/fullscreen boundary có chủ ý. `npm run test:ui-auth-public` pass 2/2.
- [x] 2026-08-14: Changelog public tiếp tục migrate `app-page-header`, `app-toolbar`, `app-button`, `app-empty-state` và shared skeleton; tìm kiếm, clear filter, loading timeline và no-result state giữ nguyên semantics. Mobile QR login bổ sung dark-mode cho các state sau scanner và dùng shared CTA. `npm run test:ui-auth-public` pass 3/3; typecheck, lint, guardrail và `git diff --check` pass.
- [x] Static implementation handoff: toàn bộ các dòng Phase 3 đã đánh dấu có code migration + contract/regression evidence tương ứng; các checkbox runtime còn mở bên dưới là kiểm chứng phụ thuộc credential, kích thước viewport hoặc môi trường emulator/production, không phải phần code còn thiếu của checkout này.
- [ ] Standards visual/interaction smoke: kiểm tra light/dark, desktop/mobile; menu “Chức năng” (outside click + Escape + file picker), segmented status tabs, view toggle, search, badge mua sắm; Detail header actions/tabs/load-more/empty state; Usage toolbar/filter chips/load-more/empty-state clear-filter/export option cards trước khi đánh dấu thêm `Button/footer`, `Modal shell` hoặc `Empty/skeleton`.
- [x] Phase 3 applicability đã được chuẩn hóa: Dashboard `Modal shell` = `N/A` vì không có generic modal cần migrate; Preparation `Toolbar`/`Modal shell`/`Empty/skeleton` = `N/A` vì màn tính toán hiện không có search/filter toolbar, generic modal hoặc loading/empty-state semantic tương ứng; Daily Checklist `Modal shell` = `N/A` vì không có generic modal. Không thêm primitive chỉ để lấp bảng.
- [x] Inventory `Modal shell` = `Exception`: giữ bottom-sheet responsive hiện tại (`items-end md:items-center`, `rounded-t-2xl md:rounded-2xl`) trên boundary `appModalA11y` để bảo toàn hành vi mobile thay vì ép qua `app-modal-shell`.
- [x] Documents `Modal shell` = `Exception`: giữ preview tài liệu trên fullscreen viewer boundary hiện tại cho tới khi shared shell hỗ trợ fullscreen workspace/header tùy biến mà không làm mất kích thước 100dvh, mobile action menu, PDF/Excel/media viewport và fullscreen API hiện hữu.
- [x] ESLint phạm vi Phase 2 (`src/app/shared/components/ui/**/*.ts`, `app.routes.ts`, `app.component.ts`) pass.
- [x] Browser smoke tại `http://127.0.0.1:4200/#/__ui-primitives`: light/dark toggle thêm class `dark`; modal có `role="dialog"`, title/body/footer; focus ban đầu vào nút `Đóng`; Escape và nút `Đóng` đều đóng modal và khôi phục focus về `Mở modal demo`.
- [x] 2026-08-14: Browser smoke bổ sung tại `http://127.0.0.1:4200/#/privacy-policy`: shared page header hiển thị một heading chính và nút `Quay lại`; tại `#__ui-primitives` xác nhận dark preview, modal mở đúng `role="dialog"`, close button khôi phục focus về `Mở modal demo`, và console không có error sau reload.
- [x] Full `npm.cmd run lint` pass sau khi chuẩn hóa 4 annotation TypeScript baseline (`array-type`/`prefer-const`) ở `smart-batch-firestore-rules.emulator.test.ts`, `prep-calculation.engine.ts`, `smart-prep.component.ts` và `standards-internal-id-sync-modal.component.ts`.
- [ ] Visual smoke light/dark cho Config shell, ba manager và Roles/Users ở kích thước desktop/mobile.
- [ ] Keyboard-only smoke cho các control đã chạm, đặc biệt modal shell mới của Config/SOP và QR lookup.
- [x] Unauthenticated local login smoke ở `http://127.0.0.1:4200/` pass tại 1280×720; console chỉ có cảnh báo service worker do dev mode.
- [ ] Authenticated local runtime smoke; local app hiện trỏ `projectId: lims-cloud-by-otada` và không cấu hình Auth Emulator, nên không dùng credential local-only để chạm Firebase production/external service. Source/build không chứng minh quyền truy cập hoặc dữ liệu Firestore.
- [ ] Emulator/production verification; không thuộc bằng chứng của change set này.

## Bàn giao cho người tiếp nhận — 2026-08-14

Phạm vi bàn giao của checkout này gồm toàn bộ thay đổi đang có trên `main`: shared UI primitives và guardrails, migration UI theo module, SmartBatch Step 2 follow-up, service-worker recovery guard, monthly-stats atomic increment/Rules test và các checklist liên quan. Không tách các file này thành một patch UI thuần vì các contract test và Rules coverage đi cùng để bảo vệ hành vi.

### Điểm vào chính

- Quy ước và ranh giới UI: [UI_CONVENTIONS.md](UI_CONVENTIONS.md).
- Trạng thái migration/open items: checklist này, đặc biệt bảng Phase 3 và các mục runtime ở phần Verification evidence.
- Primitive dùng chung: `src/app/shared/components/ui/`.
- Guardrail: `scripts/ui-guardrails.js`, `scripts/ui-overlay-baseline.json`, `package.json` (`test:ui-*`).
- SmartBatch: `src/app/features/batch/` và [SMART_BATCH_STEP2_SAMPLE_GROUP_WIZARD_CHECKLIST.md](SMART_BATCH_STEP2_SAMPLE_GROUP_WIZARD_CHECKLIST.md).
- Service worker: `src/app/core/utils/service-worker-recovery.ts` và [SERVICE_WORKER_UPDATE_LOOP_FIX_CHECKLIST.md](SERVICE_WORKER_UPDATE_LOOP_FIX_CHECKLIST.md).

### Đã kiểm tra trên checkout hiện tại

- `npm.cmd test` — pass toàn bộ suite hiện tại; Rules emulator pass 22/22, nhưng log emulator vẫn có cảnh báo giới hạn expression evaluation trong một số phép deny dự kiến.
- `npm run test:ui-contracts` — pass toàn bộ contract UI tổng hợp: primitive 5/5; Dashboard 2/2; Inventory 2/2; Results 4/4; Recipes 1/1; Preparation 1/1; Standards 5/5; Targets 2/2; Documents 2/2; Checklist 2/2; Config 7/7; SOP 2/2; Traceability 1/1; Auth/public 3/3. Lệnh này đã được gọi trong full `npm test`.
- `npm run test:ui-guardrails` — 277 file production, 0 shade không hợp lệ, 0 `fa-times`, 35 overlay legacy đúng baseline.
- `npx.cmd ngc -p tsconfig.app.json --noEmit --pretty false` và `npm.cmd run typecheck:api` — pass.
- `npm.cmd run build`, `npm.cmd run validate:release-notes` và `git diff --check` — pass; build output tại `dist/lims-cloud-pro`, release notes xác nhận `v26.08.14-b01`.
- Full `npm run lint` pass trên checkout hiện tại.

### Việc người tiếp nhận cần làm tiếp

1. Chạy visual smoke light/dark, narrow screen và keyboard-only cho các module còn mở; ưu tiên Standards, Inventory modal boundary và ba manager.
2. Tiếp tục Phase 3 theo bảng module, chỉ đánh dấu sau khi có code + contract/regression + browser evidence; không gộp authenticated/production proof vào static proof.
3. Thực hiện authenticated local smoke bằng cơ chế credential được phê duyệt, sau đó ghi riêng kết quả UI nghiệp vụ, Firebase Rules và consumer (Daily Checklist/results/print).
4. Đối với service worker, xác nhận production `UNRECOVERABLE_STATE`/`VERSION_READY` và FCM sau deploy theo checklist riêng; các mục này vẫn để mở.
5. Khi mở PR tiếp theo, đối chiếu `UI_CONVENTIONS.md`, chạy `npm.cmd run test:ui-guardrails`, cập nhật checklist và ghi rõ runtime nào chưa chạy.
